# cloudera-quickstart-starter

more simple starter kit for [cloudera/quickstart](https://hub.docker.com/r/cloudera/quickstart/)

`work/` will be mounted on `/root/work`

## Requirements

* docker
* docker-compose

## Usage

```
docker-compose run cloudera-quickstart-starter
```
